<div class="card-panel">
	<%= simple_form_for(@object, url: reset_password_sessions_path, html: { id: 'object-form' }) do |f| %>

		<%= f.input :email %>

		<button type="submit" class="waves-effect waves-light btn">Reset Password</button>

	<% end %>
</div>

<%= link_to('Return to Login', [:new, :session]) %>
